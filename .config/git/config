[user]
	name = Kento Shirasawa
	email = shirasawa.kento@gmail.com
    username = SiraKen ; git-it

[alias]
    cm = commit
    co = checkout
    br = branch
    st = status
    ps = push
    pl = pull
    a = add

[color]
ui = true

[init]
defaultBranch = main

[core]
	autocrlf = input
    ignorecase = false
    editor = nvim

[push]
; default = current

[pull]
; ff = only
; rebase = false

[credential]
; helper = osxkeychain

[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
